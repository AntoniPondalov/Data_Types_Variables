function requiredReading(numOfPages,pagesPerHour,daysNum){
    let totalHours = numOfPages / pagesPerHour;
    let requiredHours = totalHours / daysNum;

    console.log(requiredHours)

}
requiredReading (212, 20 , 2)